services:
  anvil:
    image: ghcr.io/foundry-rs/foundry:latest
    ports:
      - "8545:8545"
    command: >
      anvil
      --fork-url https://ethereum.reth.rs/rpc
      --host 0.0.0.0
      --port 8545
